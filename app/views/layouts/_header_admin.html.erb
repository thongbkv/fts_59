<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to t("static_pages.home"), root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= t "static_pages.account" %><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("static_pages.profile"), "#" %></li>
                <li><%= link_to t("static_pages.setting"), "#" %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to t("static_pages.logout"), logout_path, method: :delete %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to t("static_pages.login"), login_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</nav>
